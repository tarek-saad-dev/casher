# Booking Phase 4 — Any-barber mode

`empId` omitted → `mode: any_barber`. Engine `collectAllCandidates` + merge by `time|dayOffset`. Empty eligible set → `NO_ELIGIBLE_BARBER`. Days include `eligibleBarberCount` / `availableBarberCount`.
